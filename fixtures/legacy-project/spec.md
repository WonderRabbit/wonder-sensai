# 주문 조회 내부 명세

이 문서에서 대문자로 쓴 `MUST`, `SHOULD`, `MAY`만 BCP 14(RFC 2119, RFC 8174)의 규범 키워드로 해석한다. 소문자 표현과 코드 예시는 규범 키워드가 아니다.

## REQ-0001

주문 목록 화면은 `GET /api/orders`를 호출해야 한다(`MUST`). 성공 응답은 HTTP 200이어야 한다(`MUST`).
